---
title: "sql-视图创建"
date: "2024-05-03"
description: ""
---

![image.png](https://raw.githubusercontent.com/k1t0111/blog/main/image/20240503173030.png)

* * *

```sql
### SQL CREATE VIEW 语法CREATE VIEW view_name ASSELECT column1, column2, ...FROM table_nameWHERE condition;with check option**参数说明：**- **CREATE VIEW:** 声明你要创建一个视图。- **view_name:** 指定视图的名称。- **AS:** 指定关键字，表示视图的定义开始。- **SELECT column1, column2, ...:** 指定视图中包含的列，可以是表中的列或计算列。- **FROM table_name:** 指定视图从哪个表中获取数据。- **WHERE condition:** 可选部分，用于指定筛选条件，限制视图中的行。**注释：**视图总是显示最新的数据！每当用户查询视图时，数据库引擎通过使用视图的 SQL 语句重建数据。
```

### [](#删除视图 "删除视图:")删除视图:

```sql
drop view name;   
```

## [](#更新视图 "更新视图")更新视图

> 其实此时 是一种怎么说呢 ?  
> 就是 更新就是 update delete insert 嘛  
> 此时就是需要考虑在创建 时的with check option  
> 很重要的一步 视图消解 有个with check option  
> 在更新视图时候就会考虑创建视图时候的  
> 条件

## [](#看看实力 "看看实力")看看实力

```sql
create view LS as select * from s;create view a as select * from spj;create view b as select * from s;
```
